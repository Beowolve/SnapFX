/**
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  docsSidebar: [
    "intro",
    "getting-started",
    "architecture",
    "api-reference",
    "release-status"
  ]
};

module.exports = sidebars;
